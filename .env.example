# apps/api/.env — copy this to apps/api/.env and fill in real values

# Supabase connection string (Project Settings -> Database -> Connection string)
DATABASE_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres"

# Google AI Studio key for Gemini Flash (free tier, no card) — used by M1's parse.ts
GEMINI_API_KEY="your-gemini-api-key"

PORT=4000
